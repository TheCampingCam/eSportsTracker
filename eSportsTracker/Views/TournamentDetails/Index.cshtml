@model IEnumerable<eSportsTracker.Models.TournamentDetail>

@{
    ViewBag.Title = "Tournaments";
}

<h2>Index</h2>

<p>
    @if (Session["LoggedIn"] != null)
    {
         @Html.ActionLink("Create New", "Create")
    }
</p>
<table class="table">
    <tr>
        @{if (Session["LoggedIn"] != null)
        {
            <th>
                TournamentID
            </th>
    }}
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Organizer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Game)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        @{if (Session["LoggedIn"] != null)
        {
            <td>
                @Html.DisplayFor(modelItem => item.TournamentID)
            </td>
    }}
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Organizer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Game)
        </td>
        <td>
            @{
            if (Session["LoggedIn"] != null)
            {
                //@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) <text>|</text>
                //@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) <text>|</text>
                //@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        }
            }
        </td>
    </tr>
}

</table>
