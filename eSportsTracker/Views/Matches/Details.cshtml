@model eSportsTracker.Models.Match

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Match</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TimePlayed)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimePlayed)
        </dd>
    </dl>
</div>
<tr>
    @foreach (var v in ViewBag.winnerS) {


    }
</tr>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.MatchID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
